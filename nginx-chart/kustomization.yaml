apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helmfile.yaml

commonLabels:
  test-label: ""

configMapGenerator:
- name: valuetest
  behavior: merge
  options:
    disableNameSuffixHash: true
  files:
  - values=values.yaml
